# dCore project adapter

For DenizenScript, DenizenM, Minecraft resource-pack, shader, lint, or build work, read the installed `dcore/SKILL.md` (resolve its installation directory in this editor) and only the references it routes to. Pin exact target versions and keep `STATIC_OK` separate from client/runtime evidence. Keep this file short; canonical instructions belong in the skill.
