# dCore project adapter

For DenizenScript, DenizenM, Minecraft resource-pack, shader, lint, or build work, read `skill/dcore/SKILL.md` and only the references it routes to. Pin exact target versions and keep `STATIC_OK` separate from client/runtime evidence. Keep this file short; canonical instructions belong in the skill.
